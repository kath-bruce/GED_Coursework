
public abstract class CharacterCreator {
    
    protected abstract Character spawnCharacter(String type);
    
    public Character createCharacter(String type) {
        Character character = spawnCharacter(type);
        
        //additional methods to go with creating a character placed here
        
        return character;
    }
    
    
}
